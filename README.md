# StoryBit — Streaming Dashboard Clone (Next.js 14 + TypeScript + Tailwind)

A simplified streaming dashboard similar to Netflix/Hulu.  
Built using **Next.js 14 App Router**, **TypeScript**, **Tailwind CSS**, and **TMDB API**.

---

## 🚀 Features

- Server-side data fetching using Next.js App Router Server Components
- Hero Banner using Next `<Image fill priority />`
- Reusable Movie Rows with smooth horizontal scrolling
- Dynamic movie detail page (`/movie/[id]`)
- Fully responsive UI
- Environment variable protection
- Clean folder structure with TypeScript interfaces

---

## 📦 Installation & Setup

### 1. Clone Repo
```bash
git clone <your_repo_url>
cd storybit-streaming-dashboard
